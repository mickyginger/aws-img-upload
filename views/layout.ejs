<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>file-upload-with-views</title>
  </head>
  <body>
    <% for(const type in messages) { %>
      <div class="<%= type %>">
        <h5><%= messages[type] %></h5>
      </div>
    <% } %>
    <main>
      <%- body %>
    </main>
  </body>
</html>